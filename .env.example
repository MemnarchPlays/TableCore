# Chronicle Combat — environment variables
# Copy this file to .env and update the values.

# PostgreSQL connection (Docker Compose uses service name "postgres" as host)
DATABASE_URL="postgresql://tablecore:tablecore@postgres:5432/tablecore"

# For local dev without Docker, point at localhost:
# DATABASE_URL="postgresql://tablecore:tablecore@localhost:5432/tablecore"
